import 'package:flutter/material.dart';

class ThiredScreen extends StatelessWidget {
  const ThiredScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("The 3rd Screen"),
      ),
    );
  }
}
